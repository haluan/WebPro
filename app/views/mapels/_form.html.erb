<%= form_for(@mapel) do |f| %>
  <% if @mapel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mapel.errors.count, "error") %> prohibited this mapel from being saved:</h2>

      <ul>
      <% @mapel.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :kode %><br>
    <%= f.text_field :kode %>
  </div>
  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="actions">
    <%= f.submit class:"btn btn-primary"%>
  </div>
<% end %>
